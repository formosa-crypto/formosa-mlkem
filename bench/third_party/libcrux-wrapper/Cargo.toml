[package]
name = "libcrux-bench-wrapper"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
libcrux-ml-kem = { version = "0.0.8", features = ["std", "pqcp", "rand", "simd256"] }

[profile.release]
lto = false
