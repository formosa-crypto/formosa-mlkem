#ifndef POLY_ADD_HH
#define POLY_ADD_HH

#include "params.jahh"

fn poly_add2(reg ptr u16[KYBER_N] rp bp) -> stack u16[KYBER_N]
{
  reg u16 a;
  reg u16 b;
  reg u16 r;
  inline int i;

  for i = 0 to KYBER_N {
    a = rp[i];
    b = bp[i];
    r = a + b;
    rp[i] = r;
  }
  return rp;
}

#endif
