require "kem.jinc"

export fn jade_kem_kyber_kyber768_amd64_avx2v_keypair(reg u64 public_key secret_key) -> reg u64
{
  reg u64 r;
  stack u8[KYBER_SYMBYTES*2] randomness;
  reg ptr u8[KYBER_SYMBYTES*2] randomnessp;

  public_key = public_key;
  secret_key = secret_key;

  randomnessp = randomness;
  randomnessp = #randombytes(randomnessp);
  __crypto_kem_keypair_jazz(public_key, secret_key, randomnessp);
  ?{}, r = #set0();
  return r;
}
